<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
    <!-- Custom CSS -->
    <link rel="stylesheet" href="/css/style.css" />

    <style>
        /* Mobile First Styles */
        .event-banner-wrapper {
            position: relative;
            width: 100%;
            height: 250px; /* Smaller on mobile */
            overflow: hidden;
        }

        .event-banner {
            width: 100%;
            height: 100%;
            object-fit: cover;
            mask-image: linear-gradient(to bottom, black 60%, transparent 100%);
            -webkit-mask-image: linear-gradient(to bottom, black 60%, transparent 100%);
        }

        .event-content {
            margin-top: -20px; /* Slight overlap on mobile */
            position: relative;
            z-index: 2;
        }

        /* Card Override for Dark Theme */
        .card-event {
            background-color: var(--dark-secondary, #1a1a2e) !important;
            border: 1px solid rgba(0, 217, 255, 0.1);
            border-radius: 12px;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.3);
        }

        .video-container {
            position: relative;
            padding-bottom: 56.25%; /* 16:9 */
            height: 0;
            overflow: hidden;
            border-radius: 12px;
            border: 1px solid rgba(0, 217, 255, 0.2);
            box-shadow: 0 0 15px rgba(0, 217, 255, 0.1);
        }
        .video-container iframe {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
        }

        /* Desktop Styles */
        @media (min-width: 992px) {
            .event-banner-wrapper {
                height: 450px; /* Taller on desktop */
            }
            .event-content {
                margin-top: -100px; /* Deep overlap on desktop */
            }
            .title-hero {
                font-size: 3.5rem;
            }
        }
    </style>
</head>
<body class="bg-dark text-white">
    <%- include('../partials/navbar') %>

    <!-- Hero Banner -->
    <div class="container-fluid p-0 event-banner-wrapper">
        <div class="position-absolute top-0 end-0 m-4 px-4 py-2 rounded-pill fw-bold shadow-lg <%= event.is_active ? 'bg-aqua text-dark' : 'bg-secondary text-white' %>" style="z-index: 10;">
            Status: <%= event.is_active ? 'Active' : 'Expired/Inactive' %>
        </div>

        <% if(event.banner_url) { %>
            <img src="<%= event.banner_url %>" alt="<%= event.title %>" class="event-banner">
        <% } else { %>
            <div class="w-100 h-100 bg-secondary d-flex align-items-center justify-content-center">
                <span class="text-white-50">No Banner Available</span>
            </div>
        <% } %>
    </div>

    <!-- Main Content -->
    <div class="container event-content mb-5">
        <div class="row g-4"> <!-- g-4 for consistent gap -->
            <!-- Content Column -->
            <div class="col-lg-8">
                <div class="card card-event p-4 mb-4">
                    <h1 class="display-5 fw-bold text-white mb-3 text-shadow-aqua title-hero"><%= event.title %></h1>
                    
                    <div class="d-flex flex-wrap gap-3 mb-4 text-white-50">
                        <% if(event.start_date) { %>
                            <div class="d-flex align-items-center"><i class="fas fa-calendar-alt text-aqua me-2"></i> Starts: <%= new Date(event.start_date).toLocaleDateString() %></div>
                        <% } %>
                        <% if(event.end_date) { %>
                            <div class="d-flex align-items-center"><i class="fas fa-flag-checkered text-danger me-2"></i> Ends: <%= new Date(event.end_date).toLocaleDateString() %></div>
                        <% } %>
                    </div>

                    <p class="lead text-light mb-4" style="white-space: pre-wrap; font-size: 1.1rem; line-height: 1.6;"><%= event.description %></p>
                    
                    <% if(event.video_url) { %>
                        <div class="video-container mb-4">
                            <iframe src="<%= event.video_url %>" title="Event Video" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>
                        </div>
                    <% } %>
                </div>
            </div>

            <!-- Sidebar -->
            <div class="col-lg-4">
                <div class="card card-event p-4 sticky-top" style="top: 100px; z-index: 1;">
                    <h3 class="text-white fw-bold mb-4">Event Details</h3>

                    <% if(event.game_id) { %>
                         <a href="/games/<%= event.game_id %>/play" class="btn btn-aqua w-100 mb-3 py-3 fw-bold shadow-aqua">
                            <i class="fas fa-gamepad me-2"></i> PLAY GAMES NOW
                        </a>
                    <% } else if (event.target_url) { %>
                         <a href="<%= event.target_url %>" class="btn btn-aqua w-100 mb-3 py-3 fw-bold shadow-aqua">
                            <i class="fas fa-gamepad me-2"></i> PLAY GAMES NOW
                        </a>
                    <% } else { %>
                        <button class="btn btn-secondary w-100 mb-3 py-3 fw-bold" disabled>
                            <i class="fas fa-lock me-2"></i> NO GAME LINKED
                        </button>
                    <% } %>
                    
                    <div class="text-center mt-3 border-top border-secondary pt-3">
                        <small class="text-white-50 d-block mb-2">Share this event</small>
                        <div class="d-flex justify-content-center gap-3">
                             <a href="#" class="text-white hover-aqua transition-300"><i class="fab fa-facebook fa-2x"></i></a>
                             <a href="#" class="text-white hover-aqua transition-300"><i class="fab fa-twitter fa-2x"></i></a>
                             <a href="#" class="text-white hover-aqua transition-300"><i class="fab fa-whatsapp fa-2x"></i></a>
                             <a href="#" class="text-white hover-aqua transition-300"><i class="fas fa-link fa-2x"></i></a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Footer aligns with closing body tags in layouts/footer -->
    <%- include('../layouts/footer') %>
